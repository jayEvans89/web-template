import { Meta, Story, Canvas, ArgTypes, Unstyled } from '@storybook/blocks';
import * as BackLinkStories from './back-link.stories.svelte';

<Meta of={BackLinkStories} name="Docs" />

# Back link
A simple link component for taking the user back to a previous page.

<Canvas>
  <Story of={BackLinkStories}/>
</Canvas>

## Guidelines
<Unstyled>
- Place at the top left of the page above the h1 tag.
- Provide clear and concise text that describes where the link is taking the user back to.
</Unstyled>

## Props
<ArgTypes />

