import { Meta, ArgTypes } from '@storybook/blocks';
import * as InputLayoutStories from './input-layout.stories.svelte';

<Meta of={InputLayoutStories} name="Docs" />

# Input Layout
The input layout component is a simple wrapper that houses the label, hint text and error message.

It has one slot that is used for the actual input component.

## Props
<ArgTypes/>